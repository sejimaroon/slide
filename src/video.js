/*
import ReactPlayer from 'react-player';

const Video = ({ images }) => {
  const videoUrls = images.map((image) => URL.createObjectURL(image));

  return (
    <div>
      <ReactPlayer
        url={videoUrls}
        controls={true}
        width="100%"
        height="auto"
      />
    </div>
  );
};

export default Video;
*/